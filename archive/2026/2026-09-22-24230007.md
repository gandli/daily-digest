# Cobalt Strikeの指令通信（C

- 原链: https://x.com/MalwareBibleJP/status/2102151891815985485
- 作者: 📕「マルウエアの教科書」著者 | 吉川孝志 | 増補改訂版🌟発売中 (@MalwareBibleJP)
- 时间: Mon Sep 21 21:44:01 +0000 2026
- 数据: ❤️ 21 · 🔁 4 · 💬 0

---

Cobalt Strikeの指令通信（C2通信）を、OneDrive上のファイルの読み書きに置き換えるUser-Defined C2（UDC2）の実装「OneDrive-UDC2」がGitHubで公開されています。Cobalt Strikeは通常、侵入先の端末で動くモジュール「Beacon」と、攻撃者側でそれを待ち受ける「チームサーバ」が直接やり取りします。本実装はその直接のやり取りをやめ、Beaconとチームサーバがそれぞれ相手宛ての指令や応答をOneDrive上にファイルとして置き、相手がそれを読み取る往復をC2の通信路そのものにします。Cobalt Strike単体では完結せず、チームサーバ側で動かすPython製の中継プログラムと、Beacon内で実行するBeacon Object File（BOF）形式のクライアントを組み合わせる構成です。クラウドのファイル操作APIがC2の通信経路になり得ることを示す実装です。構成から考えられる防御上の着眼点は、Microsoft Graphによるファイル操作の異常や、不審なアプリへのFiles.ReadWrite.All権限の管理者同意などです。

READMEが示す構成要素は次の4点です。OneDrive for Businessを利用するユーザーがいるMicrosoft Entra IDテナントと、Files.ReadWrite.Allのアプリケーション権限を持つアプリ、送信用と受信用で対になるinbox／outboxフォルダ（一方が書き込む先を、他方が読み取る）、OneDriveとチームサーバの間を仲介するPython製の中継プログラム、Beacon内で動作するBOF形式のクライアントです。

防御側から見ると、通信先がMicrosoftの正規サービスであるため、宛先ドメインやレピュテーションによる切り分けは効きにくくなります。手掛かりはネットワーク側の外形よりもクラウド側のログにあります。具体的には、Microsoft GraphのFiles系APIの呼び出し傾向、Files.ReadWrite.Allのアプリケーション権限への管理者同意の記録、同一アカウントの下に現れる対のフォルダと、そこで短い間隔で繰り返される小さなファイルの作成・読み取り・削除です。

本投稿は公開READMEに基づく構成の紹介と、防御上の着眼点の整理に限定し、セットアップ・ビルド・実行・配備の手順は扱いません。取り上げているのは公開されている実装そのものであり、実被害の報告を紹介したものではありません。

【要点の整理】

・Cobalt Strike向けのUser-Defined C2チャネルの実装。C2通信をOneDrive上のファイルの読み書きに置き換える。

・Beaconはチームサーバへ直接接続せず、双方が相手の置いたファイルを読み取る往復が通信路となる。

・Cobalt Strike単体では完結せず、チームサーバ側のPython製中継プログラムとBeacon内のBOFを組み合わせて動作。

・READMEが示す構成要素は、OneDrive for Businessを利用するユーザーがいるMicrosoft Entra IDテナントとFiles.ReadWrite.Allのアプリケーション権限を持つアプリ、対になるinbox／outboxフォルダ、Python製の中継プログラム、BOF形式のクライアントの4点。

・通信先が正規のクラウドサービスであるため、宛先ベースの遮断やドメイン評価では切り分けが難しい。

・防御側の着眼点は、Microsoft Graphによるファイル操作の異常、Files.ReadWrite.Allのアプリケーション権限への管理者同意、対のフォルダで繰り返される細かなファイル操作。

詳細は以下を参照：

https://github.com/nmht3t/OneDrive-UDC2

(※可能な範囲でファクトチェックは実施済なものの、元記事の精度依存や速報・要約の性質上漏れもありうるため、正確な情報は一次情報を直接参照のこと)

---

**🌐 中文翻译**

Cobalt Strike的指令通信（C2通信）被替换为通过OneDrive上文件的读写操作的User-Defined C2（UDC2）实现「OneDrive-UDC2」已在GitHub上公开。Cobalt Strike通常由在入侵目标终端上运行的模块「Beacon」和攻击者侧等待其响应的「团队服务器」直接进行交互。本实现停止了这种直接交互，而是让Beacon和团队服务器各自将针对对方的指令或响应作为文件放置在OneDrive上，并通过对方读取这些文件的往复过程来实现C2通信路径本身。Cobalt Strike单体无法独立完成，需要结合在团队服务器侧运行的Python制中继程序，以及在Beacon内部执行的Beacon Object File（BOF）格式的客户端构成。云端的文件操作API可作为C2通信路径的实现示例。从构成来看，防御上的关注点包括通过Microsoft Graph的文件操作异常、不向可疑应用授予Files.ReadWrite.All权限的管理员同意等。

README所示的构成要素为以下4点。使用OneDrive for Business的用户所在的Microsoft Entra ID租户、具有Files.ReadWrite.All应用权限的应用、用于发送和接收的对等inbox/outbox文件夹（一方写入的位置由另一方读取）、介于OneDrive和团队服务器之间的Python制中继程序、在Beacon内部动作的BOF格式客户端。

从防御侧来看，由于通信目标是Microsoft的正规服务，因此基于目标域名或信誉的区分难以奏效。线索更多在于网络侧的外形而非云侧的日志。具体而言，包括Microsoft Graph的Files系API调用倾向、Files.ReadWrite.All应用权限的管理员同意记录、同一账户下出现的对文件夹，以及在那里以短间隔重复进行的小文件创建、读取、删除。

本投稿仅限于基于公开README的构成介绍和防御关注点的整理，不涉及设置、构建、执行、部署的手续。所涉及的是公开的实现本身，并非介绍实际损害报告。

【要点整理】

・Cobalt Strike向け的User-Defined C2通道实现。将C2通信替换为OneDrive上文件的读写。

・Beacon不直接连接团队服务器，双方读取对方放置的文件往复过程成为通信路径。

・Cobalt Strike单体无法完成，需要结合团队服务器侧的Python制中继程序和Beacon内的BOF来动作。

・README所示的构成要素为，使用OneDrive for Business的用户所在的Microsoft Entra ID租户和具有Files.ReadWrite.All应用权限的应用、对等的inbox/outbox文件夹、Python制中继程序、BOF格式客户端的4点。

・由于通信目标是正规的云服务，因此基于目标的阻断或域名评估难以区分。

・防御侧的关注点为，通过Microsoft Graph的文件操作异常、Files.ReadWrite.All应用权限的管理员同意、在对文件夹中重复进行的细微文件操作。

详情请参阅以下：

https://github.com/nmht3t/OneDrive-UDC2

(※虽已在可能范围内进行了事实检查，但由于依赖原文章的精度以及快讯、摘要的性质，可能存在遗漏，因此准确信息请直接参考一手资料)

---
由 daily-digest bot 经 FxEmbed API 自动生成