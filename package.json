{
  "name": "daily-digest",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "test": "vitest run",
    "manual": "vitest run scripts/manual && tsx scripts/manual/render.mts && tsx scripts/manual/generate.mts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "@types/node": "^26.5.1",
    "@vitest/coverage-v8": "^4.1.11",
    "playwright": "^1.63.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
